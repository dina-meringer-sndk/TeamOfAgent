```markdown
# Utils Folder

This folder contains utility scripts for the Agents Factory agent, such as:
- docx_utils.py: For reading/writing .docx files
- Additional utilities for YAML/JSON validation, file management, etc.
Add new utilities here as the agent evolves.

```